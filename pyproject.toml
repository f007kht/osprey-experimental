[project]
name = "osprey-streamlit-app"
version = "0.1.0"
description = "Streamlit app for document processing with Docling"
requires-python = ">=3.9,<4.0"
dependencies = []

[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

