torch
torchvision
torchaudio
opencv-python-headless
streamlit
docling
tesserocr
pypdf  # Required for page count detection and chunked processing

# Optional dependencies for MongoDB storage and RAG features
# These are only needed if ENABLE_MONGODB=true
pymongo[srv,ocsp]>=4.6.0  # OCSP support required for MongoDB Atlas SSL/TLS
certifi>=2023.7.22  # Required for MongoDB Atlas SSL/TLS connections
urllib3>=2.0.0  # Required for modern SSL support
# Local embeddings (default)
sentence-transformers
# Remote embeddings (optional, use remote embeddings in UI)
voyageai