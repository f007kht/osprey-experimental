torch
torchvision
torchaudio
opencv-python-headless
streamlit
docling
tesserocr

# Optional dependencies for MongoDB storage and RAG features
# These are only needed if ENABLE_MONGODB=true
pymongo[srv]
# Local embeddings (default)
sentence-transformers
# Remote embeddings (optional, use remote embeddings in UI)
voyageai